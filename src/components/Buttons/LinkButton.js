import React from 'react';

const LinkButton = (props) => {
  return (
    // TODO: need a props.news to href to redirect to news
    <a className="linkButton">{'Learn more ->'}</a>
  );
}

export default LinkButton;
